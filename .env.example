# BIM-IR Agent Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# OpenAI API Configuration (REQUIRED for Blocks 1-3)
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys
# The BIM-IR agent uses GPT-4 for intent classification, parameter extraction,
# and value resolution in the NLU pipeline.
OPENAI_API_KEY=sk-your-openai-key-here

# Optional: Specify model to use (default: gpt-4-turbo)
# OPENAI_MODEL=gpt-4-turbo

# =============================================================================
# Neo4j Configuration (OPTIONAL - only for orchestrator features)
# =============================================================================
# Only needed if using the orchestrator's graph memory features
# NEO4J_URL=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=your-neo4j-password

# =============================================================================
# Qdrant Configuration (OPTIONAL - only for orchestrator features)
# =============================================================================
# Only needed if using Qdrant vector database with orchestrator
# QDRANT_HOST=localhost
# QDRANT_PORT=6333

# =============================================================================
# Notes
# =============================================================================
# - This file should be copied to .env (without .example)
# - Never commit your .env file to version control (it's in .gitignore)
# - Only OPENAI_API_KEY is required for basic BIM-IR testing
# - Other variables are optional for advanced features
